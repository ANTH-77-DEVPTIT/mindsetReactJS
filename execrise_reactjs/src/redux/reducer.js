const initialState = {

}

const rootReducer = (state = initialState, action) => {
    switch(action.type) {
        // case "HOC_BAI":
        //     //do something here
        //     return state;


        default: 
            return state;
    }
}

export default rootReducer;